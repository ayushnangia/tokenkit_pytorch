from setuptools import find_packages, setup

setup(
    name="tokenkit",
    version="0.0.1",
    description="PyTorch implementation of tokenkit for transferring language models across tokenizers",
    packages=find_packages(),
    install_requires=[],
    entry_points={
        "console_scripts": [],
    },
)